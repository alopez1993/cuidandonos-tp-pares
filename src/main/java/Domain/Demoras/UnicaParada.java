package Domain.Demoras;

import Domain.Actores.Viaje;
import

public class UnicaParada implements TipoDemora{
    private GoogleAPI googleAPI;
    public void calcularDemoras(Viaje viaje){
        this.googleAPI.demoraUnicaParada(dict.viaje);
    }
}
